{
  "description": "Claude Code: Jev decides what the compacted conversation keeps instead of a model-written summary.",
  "modules": ["./claude.js"],
  "hooks": {
    "SessionStart": [{ "matcher": "startup|resume", "hooks": [{ "type": "command", "command": "node", "args": ["${CLAUDE_PLUGIN_ROOT}/dist/cli.js", "hook"], "timeout": 30 }] }]
  }
}
